name: Teste Our Lottery

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  teste:
    runs-on: ubuntu-latest

    steps:
      - name: Teste
        run: echo "🍀 OUR LOTTERY FUNCIONOU"
